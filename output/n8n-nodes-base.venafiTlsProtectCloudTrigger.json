{
  "node_id": "n8n-nodes-base.venafiTlsProtectCloudTrigger",
  "name": "Venafi TLS Protect Cloud Trigger",
  "version": 1,
  "semantic_context": "Starts the workflow when Venafi events occur",
  "natural_language_description": "这是 Venafi TLS Protect Cloud Trigger 节点。主要用于: Starts the workflow when Venafi events occur",
  "parameters": {
    "resource": {
      "displayName": "Resource",
      "name": "resource",
      "type": "options",
      "required": true,
      "default": [],
      "description": "Choose from the list, or specify an ID using an expression. Choose from the list, or specify IDs using an expression. Choose from the list, or specify an ID using an expression.",
      "natural_language_description": "参数名: Resource | 作用: Choose from the list, or specify an ID using an expression. Choose from the list, or specify IDs using an expression. Choose from the list, or specify an ID using an expression. | 默认值: [] | (必填项)"
    },
    "triggerOn": {
      "displayName": "Trigger On",
      "name": "triggerOn",
      "type": "multiOptions",
      "required": true,
      "default": [],
      "description": "Choose from the list, or specify an ID using an expression. Choose from the list, or specify IDs using an expression. Choose from the list, or specify an ID using an expression. Choose from the list, or specify IDs using an expression.",
      "natural_language_description": "参数名: Trigger On | 作用: Choose from the list, or specify an ID using an expression. Choose from the list, or specify IDs using an expression. Choose from the list, or specify an ID using an expression. Choose from the list, or specify IDs using an expression. | 默认值: [] | (必填项)"
    }
  },
  "ids_manifest": {
    "role": "trigger",
    "input_contract": {
      "preferred_source": "{{ $json.metadata }}"
    },
    "output_contract": {
      "standardizer_logic": "return { payload: { primary: upstream } }"
    }
  }
}