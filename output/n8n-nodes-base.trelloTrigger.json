{
  "node_id": "n8n-nodes-base.trelloTrigger",
  "name": "Trello Trigger",
  "version": 1,
  "semantic_context": "Starts the workflow when Trello events occur",
  "natural_language_description": "这是 Trello Trigger 节点。主要用于: Starts the workflow when Trello events occur",
  "parameters": {
    "id": {
      "displayName": "Model ID",
      "name": "id",
      "type": "string",
      "required": true,
      "default": "",
      "description": "ID of the model of which to subscribe to events",
      "natural_language_description": "参数名: Model ID | 作用: ID of the model of which to subscribe to events | (必填项)"
    }
  },
  "ids_manifest": {
    "role": "trigger",
    "input_contract": {
      "preferred_source": "{{ $json.metadata }}"
    },
    "output_contract": {
      "standardizer_logic": "return { payload: { primary: upstream } }"
    }
  }
}