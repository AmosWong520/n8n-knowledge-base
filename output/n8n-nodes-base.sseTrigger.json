{
  "node_id": "n8n-nodes-base.sseTrigger",
  "name": "SSE Trigger",
  "version": 1,
  "semantic_context": "Triggers the workflow when Server-Sent Events occur",
  "natural_language_description": "这是 SSE Trigger 节点。主要用于: Triggers the workflow when Server-Sent Events occur",
  "parameters": {
    "url": {
      "displayName": "URL",
      "name": "url",
      "type": "string",
      "required": true,
      "default": "",
      "description": "The URL to receive the SSE from",
      "natural_language_description": "参数名: URL | 作用: The URL to receive the SSE from | (必填项)"
    }
  },
  "ids_manifest": {
    "role": "trigger",
    "input_contract": {
      "preferred_source": "{{ $json.metadata }}",
      "accepts_binary": false
    },
    "output_contract": {
      "standardizer_logic": "return { payload: { primary: upstream.data || upstream } }"
    }
  }
}