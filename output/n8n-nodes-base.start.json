{
  "node_id": "n8n-nodes-base.start",
  "name": "Start",
  "version": 1,
  "semantic_context": "Starts the workflow execution from this node",
  "natural_language_description": "这是 Start 节点。主要用于: Starts the workflow execution from this node",
  "parameters": {
    "notice": {
      "displayName": "This node is where a manual workflow execution starts. To make one, go back to the canvas and click ‘execute workflow’",
      "name": "notice",
      "type": "notice",
      "required": false,
      "default": "",
      "description": "",
      "natural_language_description": "参数名: This node is where a manual workflow execution starts. To make one, go back to the canvas and click ‘execute workflow’"
    }
  },
  "ids_manifest": {
    "role": "trigger",
    "input_contract": {
      "preferred_source": "{{ $json.payload.primary }}"
    },
    "output_contract": {
      "standardizer_logic": "return { payload: { primary: upstream } }"
    }
  }
}