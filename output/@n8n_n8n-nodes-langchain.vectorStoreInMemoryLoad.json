{
  "node_id": "@n8n/n8n-nodes-langchain.vectorStoreInMemoryLoad",
  "name": "In Memory Vector Store Load",
  "version": 1,
  "semantic_context": "Load embedded data from an in-memory vector store",
  "natural_language_description": "这是 In Memory Vector Store Load 节点。主要用于: Load embedded data from an in-memory vector store",
  "parameters": {
    "memoryKey": {
      "displayName": "Memory Key",
      "name": "memoryKey",
      "type": "string",
      "required": false,
      "default": "vector_store_key",
      "description": "The key to use to store the vector memory in the workflow data. The key will be prefixed with the workflow ID to avoid collisions.",
      "natural_language_description": "参数名: Memory Key | 作用: The key to use to store the vector memory in the workflow data. The key will be prefixed with the workflow ID to avoid collisions. | 默认值: vector_store_key"
    }
  },
  "ids_manifest": {
    "role": "source",
    "input_contract": {
      "preferred_source": "{{ $json.metadata }}"
    },
    "output_contract": {
      "standardizer_logic": "return { payload: { primary: upstream } }"
    }
  }
}